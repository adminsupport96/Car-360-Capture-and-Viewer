# Server-side only — used by api/upload-to-drive.ts. Never prefix these with
# VITE_, or they'd be bundled into the client and leak your credentials.
# See the "Google Drive save" section in README.md for how to get these.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
GOOGLE_DRIVE_FOLDER_ID=
